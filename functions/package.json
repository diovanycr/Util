{
  "name": "painelatende-functions",
  "version": "1.0.0",
  "description": "Cloud Functions do PainelAtende (scheduled reset + deleteAuth)",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "echo \"(lint pendente)\"",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions,firestore:rules"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1"
  },
  "private": true
}
